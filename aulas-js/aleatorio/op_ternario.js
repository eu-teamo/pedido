let pais = 'Alemanha';

//Operador Ternario (compara igual o IF e ELSE, porém, com menos código)
//Sintaxe =  (condição) ? o que será executada caso seja verdadeira  : o que será executada caso seja falsa
console.log((pais === 'Brasil') ? "Você é Brasileiro!" : "Você é estrangeiro!");